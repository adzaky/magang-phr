export type benefitType = {
  id: number;
  icon: React.ReactNode;
  title: string;
  desc: string;
};

export type stepType = {
  id: number;
  desc: string;
};

export type timelineProgramType = {
  id: number;
  title: string;
  desc: string;
};

export type faqType = {
  id: number;
  title: string;
  desc: string;
};
